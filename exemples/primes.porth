#include modules/std.porth
#include modules/math.porth

100 2
for while
    dup is_prime dup if
        "Le nombre " print
        swap show swap
        space "est premier\n" print
    end
    not if 
        "Le nombre " print 
        show
        space "n'est pas premier\n" print 
        end
    endfor
end